# A sample configuration for dhcpcd.
# See dhcpcd.conf(5) for details.

# Allow users of this group to interact with dhcpcd via the control socket.
#controlgroup wheel

# Inform the DHCP server of our hostname for DDNS.
#hostname

# Use the hardware address of the interface for the Client ID.
#clientid
# or
# Use the same DUID + IAID as set in DHCPv6 for DHCPv4 ClientID as per RFC4361.
# Some non-RFC compliant DHCP servers do not reply with this set.
# In this case, comment out duid and enable clientid above.
duid

# Persist interface configuration when dhcpcd exits.
persistent

# vendorclassid is set to blank to avoid sending the default of
# dhcpcd-<version>:<os>:<machine>:<platform>
vendorclassid

# A list of options to request from the DHCP server.
option domain_name_servers, domain_name, domain_search
option static_routes, classless_static_routes
# Respect the network MTU. This is applied to DHCP routes.
option interface_mtu

# Request a hostname from the network
option host_name

# Most distributions have NTP support.
#option ntp_servers

# A ServerID is required by RFC2131.
require dhcp_server_identifier

# Generate SLAAC address using the Hardware Address of the interface
#slaac hwaddr
# OR generate Stable Private IPv6 Addresses based from the DUID
slaac private

# MINIX: disable ARP.  The IPv4 duplicate-address-detection / IPv4LL probe
# sends raw ARP frames through a dedicated BPF handle, which the lwIP BPF
# device does not carry out (the send reports "Operation not supported").
# dhcpcd 10 treats that probe failure as fatal and never assigns the
# offered lease, so with ARP enabled a DHCP-configured interface stays
# unbound.  Skipping the probe lets the lease bind (verified: em0 gets its
# address and the gateway is reachable).  DAD conflict detection and the
# 169.254.x IPv4LL fallback are given up, which is acceptable for MINIX's
# single-client use.
noarp
